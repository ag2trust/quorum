# Post-refactor audit 0/6: mechanical sweep + baseline metrics

**Date:** 2026-07-06
**Commit:** `a259b5ed` (HEAD of `main`)
**Schema:** v18
**Raw metrics:** [`baseline-metrics.json`](baseline-metrics.json)

## 1. Lints

### Standard clippy (`cargo clippy --all-targets --all-features`)

**0 warnings.** Clean.

### Pedantic + nursery (`-W clippy::pedantic -W clippy::nursery`)

**~517 raw warning lines** across all compilation units. Deduplicated unique warnings:

| Crate | Unique | Auto-fixable | Notable |
|-------|--------|-------------|---------|
| `quorum-core` (lib) | 226 | 87 | Mostly `missing_docs_in_private_items`, `cast_possible_truncation`, `must_use` |
| `quorum` (bin) | 141 | 82 | `items_after_statements`, `too_many_lines`, `cognitive_complexity` |
| `quorum` (tests) | ~80 | ~30 | `needless_collect`, `manual_let_else` |
| `fake-agent` | 11 | 9 | Style only |

**Actionable subset** (would matter in a real refactor):
- `unused_async` on `spawn_reviewer` — the function has no await (reviewer.rs:56)
- `unchecked_time_subtraction` in serve/mod.rs:4052 — could panic on old systems
- `missing_const_for_fn` on `AgentProc::from_parts` and `AgentName::is_generated`

No pedantic items indicate bugs; all are style/convention.

### Command

```bash
cargo clippy --all-targets --all-features
cargo clippy --all-targets --all-features -- -W clippy::pedantic -W clippy::nursery
```

## 2. Dead code + unused dependencies

### Dead code

`RUSTFLAGS="-W dead_code" cargo check --workspace` — **0 warnings.**

### Unused dependencies

`cargo machete` — **0 unused dependencies.** All declared deps are referenced.

### Command

```bash
RUSTFLAGS="-W dead_code" cargo check --workspace
cargo machete
```

## 3. Coverage map

**Overall:** 86.16% line coverage (302 tests, 0 failures).

`cargo llvm-cov --workspace --summary-only`

### Per-file coverage (non-test source only)

| File | Lines | Missed | Cover % | Flag |
|------|-------|--------|---------|------|
| `quorum/src/serve/recovery.rs` | 382 | 300 | **21.47%** | UNDER 50% |
| `quorum/src/serve/mod.rs` | 3194 | 1115 | 65.09% | |
| `quorum/src/main.rs` | 785 | 179 | 77.20% | |
| `quorum/src/serve/session_log.rs` | 310 | 75 | 75.81% | |
| `quorum/src/serve/merge.rs` | 728 | 141 | 80.63% | |
| `quorum/src/cockpit.rs` | 476 | 70 | 85.29% | |
| `quorum/src/paths.rs` | 131 | 19 | 85.50% | |
| `quorum-core/src/claims.rs` | 282 | 20 | 92.91% | |
| `quorum-core/src/stats.rs` | 1328 | 71 | 94.65% | |
| All other files | — | — | >92% | |

### Files under 50% line coverage

1. **`quorum/src/serve/recovery.rs`** — 21.47%. Recovery logic (daemon crash recovery,
   orphan reaping) is almost entirely untested at the integration level. Unit tests exist
   for some helpers but the main `recover_orphans` and `recover_stale_workers` paths are
   not exercised.

### Command

```bash
cargo llvm-cov --workspace --summary-only
```

## 4. Stale vocabulary grep

Searched for: `kind:review`, `review_of`, `auto_resolve_review`, `REVIEW_LABEL`,
`only_repo`, `repo_dir_map`, `instance_id`, `repo_from_refs`, `DEFAULT_REPO`, `closed`
(as task status), `sticky_until`, `done` described as non-terminal.

### Findings in code

| Term | Location | Verdict |
|------|----------|---------|
| `"closed"` | `cockpit.rs:389,578` | **STALE** — not a lifecycle state. Terminals are `done`, `failed`, `cancelled`. Display code references a status that doesn't exist in the state machine. |
| `sticky_until` | `schema.sql:68-71`, `sync.rs:404-609`, `db.rs:157-159` | **LIVE** but undocumented in current design spec (`docs/2026-06-23-quorum-design.md`). Drift: code uses it for rework sticky-reopen windows, spec doesn't mention it. |
| `instance_id` | `db.rs:218-275` | **Migration history only** (v16 adds, v18 drops). Not stale — required for forward-only migration chain. |

### Findings in docs only

| Term | Location | Verdict |
|------|----------|---------|
| `kind:review`, `review_of`, `REVIEW_LABEL` | `docs/2026-06-26-review-as-task-plan.md` | Historical plan doc. Not in code. Drift risk if someone reads the plan as current. |
| `sticky_until` | `docs/2026-06-26-review-as-task-plan.md` | Same plan doc — but this term IS live in code (see above). |
| `instance_id` | `CLAUDE.md:94-95` | CLAUDE.md mentions instance_id in load-bearing invariant #11. The mention is accurate (explains what daemon_lock replaced), but could confuse since the column no longer exists. |

### Not found (fully cleaned out)

`auto_resolve_review`, `only_repo`, `repo_dir_map`, `repo_from_refs`, `DEFAULT_REPO`

### "done" as non-terminal

`docs/2026-06-23-quorum-design.md:389` states: "Terminals: done, failed, cancelled (reachable from any non-terminal)" — this correctly describes `done` as terminal. No instances of `done` described as non-terminal found.

### Command

```bash
grep -rn 'kind:review\|review_of\|...' --include="*.rs" --include="*.md" ...
```

## 5. Size + churn map

### Size (lines of code, non-test source)

| Module | Lines | Files |
|--------|-------|-------|
| `quorum-core/src/` | ~7800 | 22 |
| `quorum/src/` (bin+serve) | ~7700 | 21 |
| `quorum/tests/` | ~9700 | 28 |
| **Total** | **33,696** | **71** |

### Top files by size

| File | Lines |
|------|-------|
| `quorum/src/serve/mod.rs` | 4151 |
| `quorum-core/src/sync.rs` | 2178 |
| `quorum-core/src/stats.rs` | 1898 |
| `quorum-core/src/tasks.rs` | 1624 |
| `quorum-core/src/db.rs` | 1284 |
| `quorum/src/serve/merge.rs` | 1069 |
| `quorum/src/main.rs` | 995 |
| `quorum-core/src/lifecycle.rs` | 944 |

### Top churn files (commits since 2026-05-01)

| File | Commits | Lines | Churn x Size |
|------|---------|-------|-------------|
| `quorum/src/serve/mod.rs` | 4 | 4151 | 16604 |
| `quorum/src/main.rs` | 3 | 995 | 2985 |
| `quorum/src/serve/render.rs` | 3 | 277 | 831 |
| `quorum/src/cli.rs` | 3 | 476 | 1428 |
| `quorum/tests/cli_serve_reviewer.rs` | 2 | 765 | 1530 |
| `quorum-core/src/lifecycle.rs` | 2 | 944 | 1888 |
| `CLAUDE.md` | 2 | — | — |

**Hot spot:** `serve/mod.rs` is both the largest file (4151 lines) and highest-churn.
At 65% coverage, it's the primary risk concentration.

### Command

```bash
git log --since="2026-05-01" --name-only --pretty=format:"" | sort | uniq -c | sort -rn
wc -l $(find . -name "*.rs" -not -path "*/target/*" | sort)
```

## 6. TODO / FIXME / HACK / XXX inventory

**0 items found** across all `.rs`, `.sh`, `.toml`, and `.md` files.

```bash
grep -rn "TODO\|FIXME\|HACK\|XXX" --include="*.rs" --include="*.toml" --include="*.sh" --include="*.md"
```

## Aim-here: downstream audit scopes

### Audit 1 — Lifecycle state machine

**Focus:** `quorum-core/src/lifecycle.rs` (944 lines, 99.84% covered, 2 recent commits).
Well-covered but the highest-stakes module. Verify: all transitions are tested including
negative paths, `closed` status references in cockpit.rs align with actual terminal states,
`sticky_until` rework windows are exercised end-to-end.

### Audit 2 — Tick loop

**Focus:** `quorum/src/serve/mod.rs` (4151 lines, 65% covered, 4 recent commits).
The largest and highest-churn file. Primary coverage gap. Verify: all tick branches are
exercised, cancellation safety in `select!` branches, error classification is exhaustive.

### Audit 3 — Recovery

**Focus:** `quorum/src/serve/recovery.rs` (480 lines, **21.47% covered**).
The only file under 50% coverage. Recovery paths (orphan reaping, stale worker cleanup)
are critical for daemon resilience but almost entirely untested at the integration level.

### Audit 4 — Liveness / presence

**Focus:** `quorum-core/src/agents.rs` (629 lines, 95.88% covered),
`quorum/src/serve/agent.rs` (215 lines, 93% covered). Presence is implicit (write-touching
bumps `last_seen`). Verify: liveness window consistency, death detection paths, the
`unused_async` on `spawn_reviewer`.

### Audit 5 — Storage + CLI

**Focus:** `quorum-core/src/db.rs` (1284 lines, 97% covered — migration chain),
`quorum/src/main.rs` (995 lines, 77% covered — CLI dispatch),
`quorum-core/src/claims.rs` (429 lines, 92.9% covered).
Verify: migration chain is complete and forward-only, CLI argument validation for
uncovered branches in main.rs, claim race canary still tight.

### Audit 6 — Ops (merge, approvals, sync)

**Focus:** `quorum/src/serve/merge.rs` (1069 lines, 80.6% covered),
`quorum/src/serve/approvals.rs` (629 lines, 93.8% covered),
`quorum-core/src/sync.rs` (2178 lines, 98.4% covered).
Verify: merge-check logic handles all PR states, approval deduplication,
`sticky_until` clearing on release in sync.rs.

## Verification

`cargo test` — 302 passed, 0 failed, 0 ignored.
All numbers reproducible from the commands listed in each section.
